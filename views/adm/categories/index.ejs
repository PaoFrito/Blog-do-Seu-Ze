<%- include('../../partials/header.ejs') %>
<div class="container" style="margin-top: 32px;">
    <h2>Categorias</h2>
    <hr>
    <div class="card">
        <div class="card-header">
            <h4>Add Categoty</h4>
        <a class="navbar-brand" href="/adm/categories/new"><button class="btn btn-success">Go</button></a>
        </div>
    </div>
    <hr>
    <table class="table" style="margin-top: 64px;">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Slug</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <% category.forEach(category => { %>
                <tr>
                    <td><%= category.id %></td>
                    <td><%= category.name %></td>
                    <td><%= category.slug %></td>
                    <td>
                        <form action="/adm/categories/edit" method="POST" >
                            <input type="hidden" name="id" value="<%= category.id %>">
                            <button type="submit" class="btn btn-warning">Editar</button>
                        </form>
                        <br>
                        <form action="/adm/categories/delete" method="POST">
                            <input type="hidden" name="id" value="<%= category.id %>">
                            <button type="submit" class="btn btn-danger">Deletar</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
<%- include('../../partials/footer.ejs') %>